{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for an application that fetches stock prices from a mock API every minute and displays them. Users can select which stocks they want to view from a predefined list.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gurbeer Singh Sokhi",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
